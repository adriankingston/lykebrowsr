Local datasets live here. Any `.json` file is listed on the home page and
served at `/api/data?set=<filename-without-extension>`.

This is the door for data the public APIs don't have: personal listening
exports, scraped extras, hand-built overlays.
